# CS-Study-Notes
## 方法
### 对数器
#### 1）你想要测的方法a（最优解）
#### 2）实现复杂度不好但是容易实现的
### 滑动窗口：维持左、右边界都不回退的一段范围，来求解很多子数组（串）的相关问题
#### 滑动窗口的关键：找到*范围*和*答案指标*之间的*单调性关系（类似贪心）*
#### 滑动过程：滑动窗口可以用*简单变量*或者*结构*来维护信息
#### 求解大流程：求子数组在*每个位置开头*或*结尾*情况下的答案
#### 注意：滑动窗口维持最大值或者最小值的*更新结构*
#### 
### 字符串哈希：如何快速得到字符串中任意字串的哈希值
#### 1）选择一个质数做进制数，base
#### 2）得到base的各种次方，在自然溢出下的结果，用pow数组记录
#### 3）
### 二分答案法（分析单调性、建立f函数）
#### 1）估计*最终答案可能的范围*是什么
#### 2）分析*问题的答案*和*给定条件*之间的*单调性*
#### 3）建立一个f函数，*当答案固定的情况下*，判断*给定的条件是否达标*
#### 4）在*最终答案可能的范围上不断二分搜索*，每次用f函数判断，直到二分结束，找到*最合适的答案*
## 语言特性
#### 浮点数在计算机中存储的是近似值，float保存有精度缺失
### 小数的四舍五入（保留三位小数）：对第四位转化
#### a=floor(a*1000+0.5)/1000; 
#### //假如这个小数位的数值大于5，加上0.5能够实现进位的作用，倘若小于0.5，就没有实现进位的作用。如此，就能实现小数位的四舍五入。
#### //floor为double型函数，用于向下取整
#### //C语言的隐式转换规则：当 double 和 int 进行算术运算时，int 会自动提升为 double，所以 / 100 等价于 / 100.0，除法按双精度浮点执行
### 判断浮点数是否相等:判断两个浮点数的差是否小于一个很小的容忍误差（如 1e-9），(不能直接用==！)
#### if (fabs((a - b) - c) < 1e-9) 
#### //fabs(x):返回 x 的绝对值，返回值类型为 double (abs用于整数！）
### 数论
#### 1.绝对值的和 |a| + |b| 有一个重要的数学性质：
#### 对于任意实数 a,b，等式|a| + |b| = max{ |a+b|, |a-b| }恒成立。
#### 2.设 𝑎,𝑏 是不全为零的整数，则存在整数 𝑥,𝑦 使得𝑎𝑥+𝑏𝑦=gcd⁡(𝑎,𝑏)并且 gcd(𝑎,𝑏) 是能写成 𝑎𝑥+𝑏𝑦 形式的最小正整数。
#### gcd:while(b){t=a%b;a=b;b=t;} return a;
